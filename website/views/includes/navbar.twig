{# {% block navbar %} #}
<nav class="w-full navbar">
    <div class="container mx-auto px-4">
        <div class="flex items-center justify-between py-2">
            <div>
                <a href="/">
                    <img class="w-10" src="https://media.discordapp.net/attachments/901980905579643004/1099463713125310526/emoji.png" alt="Logo">
                </a>
            </div>
            <div class="flex">
                <a href="#" class="text-gray-800 font-semibold px-1.5">Commandes</a>
                <a href="#" class="text-gray-800 font-semibold px-1.5">Dashboard</a>
                <a href="#" class="text-gray-800 font-semibold px-1.5">Contact</a>
            </div>
            {% if user is defined %}
                <div class="flex justify-between items-center">
                    <img src="https://cdn.discordapp.com/avatars/{{ user._id }}/{{ user._avatarHash }}" class="w-10 h-10 rounded-full">
                    <a href="/dashboard" class="text-gray-800 font-semibold border px-4 py-1 rounded-lg">{{ user.username }}</a>
                    <a href="/logout" class="text-gray-800 font-semibold border px-4 py-1 rounded-lg">Logout</a>
                </div>
            {% else %}
                <div class="flex justify-between items-center">
                    <a href="/login" class="text-gray-800 font-semibold border px-4 py-1 rounded-lg">Login</a>
                </div>
            {% endif %}
        </div>
    </div>
</nav>
{# {% endblock %} #}